# Chart defaults are correct: installCRDs true, single controller replica,
# validating webhook disabled (snapshot validation is CEL-based since
# external-snapshotter v8). ServiceMonitor left off.
#
# VolumeSnapshotClasses are deliberately NOT set here — define one alongside
# whatever consumes CSI snapshots (none currently).
controller:
  replicaCount: 1
