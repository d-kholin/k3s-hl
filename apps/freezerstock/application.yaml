# Argo CD Application for FreezerStock (freezer inventory tracker).
# Workload path: apps/freezerstock/manifests

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: freezerstock
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/d-kholin/k3s-hl.git
    targetRevision: HEAD
    path: apps/freezerstock/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: freezerstock
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    # k8up-schedule.yaml needs the K8up CRDs (wave-1 infra app) — skip dry-run
    # + retry cover first sync on a fresh cluster.
    syncOptions:
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
