apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: teslamate
  labels:
    app: grafana
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
