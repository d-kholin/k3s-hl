apiVersion: v1
kind: Service
metadata:
  name: bentopdf
  namespace: bentopdf
  labels:
    app: bentopdf
spec:
  type: ClusterIP
  selector:
    app: bentopdf
    app.kubernetes.io/name: bentopdf
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
