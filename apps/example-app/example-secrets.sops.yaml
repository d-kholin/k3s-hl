# Sample Kubernetes Secret for KSOPS.
# MUST be encrypted with: sops --encrypt --in-place example-secrets.sops.yaml
# before committing real secret values. Dummy values below are placeholders only.

apiVersion: v1
kind: Secret
metadata:
  name: example-app-secrets
  labels:
    app: example-app
type: Opaque
stringData:
  # Dummy values — replace and encrypt with SOPS before use
  EXAMPLE_USERNAME: example-user
  EXAMPLE_PASSWORD: change-me-before-encrypting
